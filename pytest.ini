[pytest]
testpaths = tests
addopts = -ra
filterwarnings =
    error
    # matplotlib/pandas emit harmless deprecations on some versions; the test
    # suite must not depend on them, so leave them visible but not fatal for the
    # analysis modules only.
    default:::analysis.*
